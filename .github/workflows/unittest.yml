name: Unit Test

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Brace Unit Tests
        run: |
          cd tests
          wget -O phpunit https://phar.phpunit.de/phpunit-9.phar
          chmod +x phpunit
          php phpunit
